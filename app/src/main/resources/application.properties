# OpenFDA Configuration
openfda.api.key=${OPENFDA_API_KEY:}  # Set via environment variable
openfda.api.timeout=10  # Timeout in seconds

# Reactive WebClient Configuration
spring.webflux.base-path=/
spring.codec.max-in-memory-size=2MB
